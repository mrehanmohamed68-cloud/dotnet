using System;

namespace day9_csharp
{
    // Underlying type changed from the default int (4 bytes) to byte (1 byte).
    public enum Gender : byte
    {
        Male = 0,
        Female = 1
    }
}
